language: ruby
rvm:
  - 1.8.7
  - 1.9.3
notifications:
  disabled: true
before_install:
 - gem install ffi
 - gem install pygments.rb
 - printenv
 - python --version
 - which python
 - ruby -e "require 'rubygems'; require 'pygments.rb'"
